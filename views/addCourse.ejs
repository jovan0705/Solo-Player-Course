<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Course</title>
    <style>
        * {
            margin: 0;
            padding: 0;
        }

        body {
            background-color: skyblue;
        }

        label {
            color: whitesmoke;
            font-size: 30px;
        }


        .isi {
            width: 80%;
        }


        p {
            color: white
        }

        h2 {
            font-size: 40px;
            color: white;
            border: 5px solid black;
            width: 35%;
            margin-top: 2%;
            margin-left: 32.5%;
            background-color: orangered;
            border-radius: 20px;
            padding: 5px;
            text-align: center;
        }

        .isi {
            width: 100%;
            margin: 10em auto auto auto;
            text-align: center;
        }

        .nav {
            background-color: orangered;
            padding: 3px 8px 3px 8px;
            border-radius: 10px;
            font-size: 25px;
            margin-top: 1%;
            margin-bottom: 1%;
        }

        .nav1 {
            padding: 3px 8px 3px 8px;
            border-radius: 10px;
            font-size: 30px;
        }

        .errors {
            font-size: 20px;
            background-color: skyblue;
            padding: 10px auto 10px auto;
            width: 50%;
            margin-left: 25%;
            border-radius: 10px;
        }

        .errors p {
            color:red;
        }
    </style>
</head>
    <h2>Course Selected: <%=data2.name%>
    </h2>
<div class='isi'>
    
    <div class='errors'>
        <%if (errors !==undefined) {%>
            <%errors.forEach(el=> {%>
                <p>
                    <%=el%>
                </p>
                <%})%>
                    <%}%>
    </div>
    <form action="/user/<%=id%>/addCourse/<%=CourseId%>" method="post">
        <select name="TeacherId" id="TeacherId" class='nav1'>
            <option disabled selected value>Teachers</option>
            <%data.forEach(el=> {%>
                <option value="<%=el.id%>">
                    <%=el.name%>
                </option>
                <%})%>
        </select>
        <br>
        <input type="submit" value="submit" class='nav'>
    </form>
    <a href="/user/<%=id%>/addCourse" class='nav'>Back</a>
</div>

</body>

</html>