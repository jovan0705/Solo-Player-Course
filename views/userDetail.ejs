<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <style>
        body {
            text-align: center;
            margin-left: auto;
            margin-right: auto;
            width: 100%;
            background-color: skyblue;
        }
        .isi {
            margin-top: 1%;
            margin-left: 41%;
        }
        .card {
            box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
            transition: 0.3s;
            width: 30%;
            background-color: orangered;
        }

        .card:hover {
            box-shadow: 0 8px 16px 0 rgba(0, 0, 0, 0.2);
        }

        .container {
            padding: 1px 16px;
        }

        img {
            width: 100%;
            height: auto;
        }

        h5 {
            padding: 0;
            margin-top: 5px;
            font-size: 20px;
            margin-bottom: 5px;
        }

        p {
            margin: 5px;
        }

        .nav {
            background-color: orangered;
            padding: 3px 8px 3px 8px;
            border-radius: 10px;
            font-size: 20px;
        }

        .errors {
            font-size: 20px;
            background-color: skyblue;
            padding: 10px auto 10px auto;
            width: 50%;
            margin-left: 25%;
            border-radius: 10px;
        }

        .errors p {
            color:red;
        }
    </style>
</head>

<body>
    <div class='header'>
        <h1>Hello <%=data[0].name%> This is Your Profile Details</h1>
        <a href="/user/<%=data[0].User.id%>/detail/edit" class = 'nav'>Edit</a>
        <a href="/user/<%=data[0].User.id%>" class = 'nav'>Back</a>
    </div>
    <div class='isi'>
        <div class='card'>
            <img src="<%=data[0].profilePicture%>" alt="Profile Pictures" width="20%">
            <div class='container'>
                <h5>Name:</h5>
                <p><%=data[0].name%></p> 
                <h5>Age: </h5>
                <p><%=data[0].age%></p>
                <h5>Email: </h5>
                <p><%=data[0].email%></p>
                <h5>Gender: </h5>
                <p><%=data[0].gender%></p>
            </div>
        </div>
    </div>


</body>

</html>